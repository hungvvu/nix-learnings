let
  f = x: x + 1;
in f

# It is possible to assign a name to a function like with other variables.
# This code still returns <LAMBDA>, since that is the value of the function.
# However, this makes it easier to refer to the function within the code.
